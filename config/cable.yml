development:
  adapter: async

test:
  adapter: test

production:
  adapter: <%= ENV.fetch("REDIS_URL", nil) ? "redis" : "async" %>
  url: <%= ENV.fetch("REDIS_URL", "redis://localhost:6379/1") %>
  channel_prefix: taskello_production
